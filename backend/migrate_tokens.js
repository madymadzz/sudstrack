require('dotenv').config(); const pool = require('./config/db'); async function migrate() { try { await pool.query('CREATE TABLE IF NOT EXISTS password_reset_tokens (token_id SERIAL PRIMARY KEY, account_id INT NOT NULL REFERENCES accounts(account_id) ON DELETE CASCADE, token_hash VARCHAR(255) NOT NULL, expires_at TIMESTAMP NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);'); console.log('Success'); } catch(e) { console.error(e); } finally { process.exit(0); } }; migrate();
